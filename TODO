# TODO LIST:
1. Implement Input Validation(passwords, names, ...), Error Handling
2. Binary file upload/download
3. Background task scheduling (
    => (notification) or 
    => (week statistics) or
    maybe possible(custom events) or
    ...
)